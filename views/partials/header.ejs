<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<style>body {
        margin: 0;
        font-family: 'Roboto', sans-serif;
        background-color: black; /* Dark background */
        color: #7f9eb9; /* Light text */
    }



    .site-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #0a0a0a; /* Slightly lighter dark shade for header */
        padding: 10px 20px;
    }

    .header-logo a {
        color: #7f9eb9;
        text-decoration: none;
        font-size: 1.5em;
        font-weight: bold;
    }

    .header-nav ul {
        list-style: none;
        padding: 0;
        display: flex;
    }

    .header-nav ul li {
        margin: 0 20px;
    }

    .hover {
        position: relative;
        color: #7f9eb9;
        text-decoration: none;
        transition: color 0.3s ease;
        display: inline-block; /* Ensure proper alignment */
        padding: 5px; /* Add some padding */
        top: 5px;
    }

    .hover:hover {
        -webkit-box-reflect: below 1px linear-gradient(transparent, #0005);
        transition: 0.3s ease-in;
    }


    .hover::before {
        content: '';
        background: linear-gradient(to top, rgba(173, 216, 230, 0.10), rgba(173, 216, 230, 0));
        position: absolute;
        left: 0;
        bottom: 3px;
        width: 100%;
        height: 2px;

        transition: all .3s ease-in-out;
    }

    .hover:hover::before {
        -webkit-box-reflect: below 1px linear-gradient(transparent, #0005);
        bottom: 0;
        height: 100%;
    }

    .header-auth {
        display: flex;
        justify-content: center;
        align-items: center;
        margin-right:10px;
    }

    .header-auth a {
        padding: 5px;
        color: #7f9eb9;
        text-decoration: none;
        transition: color 0.3s, color 0.3s;
    }

    .header-auth a:hover {
        color: lightblue;
    }

    #form-div {
        display: flex;
        flex-direction: row;
        flex-grow: 2;
    }

    #form {
        width: 100%;
    }

    #query {
        padding-left: 10px;
        color: lightblue;
        border-radius:10px;
        border: 1px solid lightblue;
        background-color: transparent;
    }

    .login-button {
        background-color: transparent;
    }

    .signup-button {
        background-color: #1f1f1f;
    }

    @media screen and (max-width: 1000px) {
        body {
            overflow-x: hidden;
        }
        .header-logo {
            display: none;
        }

        #about, #services {
            display: none;
        }
        #form {
            width: 100%;
            margin-left: 10px;
            margin-right:10px
        }
        #query {
            width:85%;
        }
    }
    @media screen and (max-width: 640px) {
        body {
            overflow-x: hidden;
        }
        .header-logo {
            display: none;
        }

        #about, #services {
            display: none;
        }
    }
</style>
<body>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>



<header class="site-header">
    <div class="header-logo">
        <a href="/"><img src="/img/MMDb logo cropped and keyed.png" width="150.66" height="53" loading="lazy" alt="logo"></a>
    </div>
    <div class="header-nav">
        <ul>
            <li><a href="/dashboard" class="hover">Home</a></li>
            <li id="about"><a href="/about" class="hover">About</a></li>
            <li id="services"><a href="#services" class="hover">Services</a></li>
            <li><a href="#socials" class="hover">Contact</a></li>
        </ul>
    </div>
    <div id="form-div">
        <form role="search" id="form" action="/search" method="post">
            <input type="search" id="query" name="q" placeholder="Search..." aria-label="Search through site content">
            <button id="searchButton" style="all: unset; margin-left: 10px; width: 44px; height: 44px;">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                    <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0"/>
                </svg>
            </button>
        </form>
    </div>
    <div class="header-auth">
        <% if (isAuthenticated) { %>
            <a href="/profile" class="hover"><span><%= user.username%></span></a>
            <a href="/profile"><img src="<%= user.profile !== 'null' ? user.profile : '/img/defaultProfile.jpg' %>" alt="profile" width="60px" height="60px" loading="lazy" /></a>
            <a href="/signout" class="hover">Signout</a>
    <% } else { %>
            <a href="/login" class="login-button">Login</a>

        <% } %>
    </div>
</header>

</body>
</html>
